# Private
Nothing here, go away

## Jinja environment setup
To prepare a virtual environment with the required Jinja version, run:

```bash
./scripts/setup_jinja_env.sh
```

After running the script, activate the environment with:

```bash
source .venv/bin/activate
```

This lets you check Jinja templates using the pinned dependencies.
